<%= form_with(model: as_run, local: true) do |form| %>
  <% if as_run.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(as_run.errors.count, "error") %> prohibited this as_run from being saved:</h2>

      <ul>
      <% as_run.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="field">
    <%= form.label :attachment %>
    <%= form.text_area :attachment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
