<%= form_with(model: asrun, local: true) do |form| %>
  <% if asrun.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(asrun.errors.count, "error") %> prohibited this asrun from being saved:</h2>

      <ul>
      <% asrun.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
